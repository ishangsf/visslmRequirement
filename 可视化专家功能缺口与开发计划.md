# 数据可视化专家功能缺口与开发计划

本文按《AI数据可视化专家开发方案》的 MVP 范围核对当前代码。状态以可运行代码和自动化测试为准，不以阶段名称推断。

## 1. 已实现

- `DataScope`、`FieldProfile`、`QuerySpec`、`DashboardSpec` 基础类型与校验；
- 动态字段画像、类型推断、筛选、单/双维聚合、时间粒度、Top N 和资源行数限制；
- 12 类受控组件、24 列布局、四套主题、预览模式；
- `@数据可视化专家` 路由、Ollama 结构化规划、查询执行、校验和一次修复；
- 输入 `@` 后的专家候选、键盘选择、双专家显式路由和真实阶段进度；
- Artifact 消息和打开画布；
- 大屏保存、版本历史、撤销、版本恢复；
- PNG、PDF、DashboardSpec JSON 导出；
- 每个受控组件的数据口径抽屉，包含 QuerySpec、中文维度/指标/筛选说明、来源范围和刷新时间；
- 导出前的数据范围、组件/查询规模和敏感字段确认；PNG 在渲染进程固定为 1920×1080 后经受控 IPC 写入；
- 本地自动保存和恢复草稿；草稿不产生版本，工作台可显示保存状态并可清除当前大屏的本地草稿；
- 质量诊断、生成运行记录、54 条 AI 固定回归场景和导出治理逻辑回归；
- 工作台质量抽屉支持单组件确定性修复；修复会重建失效 QuerySpec、编码和数据，必要时仅调整目标布局，并在查询执行与整屏校验成功后原子提交；
- 自然语言继续修改现有大屏：标题、副标题、主题、组件增删改、图表类型、Top N、排序和时间粒度均通过受限 Patch 完成，并在失败时保持原版本；
- 画布直接拖拽、组件间位置换位、右下角缩放、布局边界/最小尺寸/碰撞校验，以及撤销和草稿联动；
- 高级查询面板：字段画像、维度与时间粒度、指标字段/聚合、筛选条件、排序和 limit 编辑，所有变更重新执行受控 QuerySpec 后才提交；
- 字段画像按数据修订号持久化，支持默认语义角色、业务显示名和别名；查询结果按稳定 QuerySpec 哈希缓存，并在同步、导入或删除数据后失效；
- 指标支持同比、环比、占比、累计计算、可配置周期偏移和受限自定义公式；查询面板可选择计算方式、对比周期与差值/百分比模式，并对缺少时间粒度的请求提前提示；
- 组件支持受控颜色编辑、主/标签/对比指标编码编辑；图表渲染器会使用编码和强调色，颜色仅允许十六进制值；
- DashboardSpec 支持最多 8 个独立全局筛选器；筛选栏值会注入所有组件 QuerySpec，按最多 4 个查询并发批次重新计算，并保留筛选来源标记；
- 本地 IPC 隔离和 QuerySpec/DashboardSpec 主进程校验。

## 2. 部分实现

| 功能 | 当前状态 | 缺口 |
|---|---|---|
| 字段目录 | 可按数据修订号持久化画像，支持显示名、别名、dimension/measure/time/identifier 角色、enum/array/object 类型和敏感级别 | 仍可继续补充冲突率等高级画像 |
| 查询能力 | 支持分组、趋势、Top N、基础聚合、同比/环比、占比、累计值、N 周期对比和受限四则公式；结果缓存会随同步、导入和删除数据失效 | 仍需补公式函数、跨粒度对比和试点口径验收 |
| 大屏生成 | 可生成标题与 3～8 个组件，并显示真实阶段进度 | 生成结果默认不主动规划筛选器，需在工作台添加 |
| 主题 | 深色科技、明亮商务、深色稳重、明亮简洁 | 需要真实试点和像素级截图验收 |
| 编辑器 | 标题、类型、单位、颜色、编码、网格数值、拖拽、缩放和高级查询面板已实现 | 还缺更丰富的主题 token 和可视化规则预览 |
| 版本 | 手动保存产生版本，可撤销、恢复、本地自动保存草稿，并支持字段/组件/QuerySpec 对比；恢复前二次确认，恢复中禁止重复操作并产生新版本 | 仍需真实用户试点记录版本回滚体验 |
| 质量 | 结构、数据、性能和部分敏感字段诊断；生成期和工作台均支持单组件自动修复，失败时不修改原大屏 | 仍需通过真实试点积累修复命中率并扩展更多可安全自动处理的告警类型 |
| 审计 | 已记录保存、恢复、诊断、单组件修复、PNG/PDF/JSON/JSONL 导出结果；可视化 Agent 运行包含字段画像、模型编排、校验、查询、Patch 和修复的受控工具明细；非保存操作记录稳定 Spec SHA-256 指纹，修复记录源/结果指纹 | 团队权限、审批和集中审计不属于当前单机单用户范围，需产品重新确认后另立方案 |

## 3. 已补齐的发布门禁 MVP 功能

- 3 个脱敏黄金业务场景和人工黄金 DashboardSpec；
- 5 万条数据下的 P95 性能门禁，以及 1366×768、1920×1080、4K 布局视觉回归矩阵；
- 四套主题 token、主题渲染覆盖和版本对比。

## 4. 第二阶段功能，当前均未进入正式开发

- 多页驾驶舱、下钻和图表联动；
- 定时刷新和演示轮播；
- 企业模板、品牌主题和组件模板市场；
- 只读分享链接或服务端发布；
- 多数据源 JOIN、SQL 数据库和 REST API；
- AI 自动讲解、异常检测和归因分析；
- 团队权限、审批和完整审计。

## 5. 后续开发顺序

1. **MVP 闭环补齐 A**：数据中心携带 `DataScope` 交给专家；真正的 @Mention 选择；阶段进度。
2. **MVP 闭环补齐 B**：组件口径抽屉；PNG 导出；导出治理确认。已完成。
3. **编辑闭环**：自然语言 Patch、拖拽缩放、高级查询面板、自动保存草稿均已完成。下一开发入口为数据底座补强。
4. **数据底座补强**：语义别名、持久化字段画像、查询缓存、同步失效以及同比/环比/占比/累计指标已完成；下一步进入发布门禁。
5. **发布门禁**：4 套主题、3 个黄金场景、性能与多分辨率视觉回归已完成；固定 Windows Electron 三视口真实截图基线已完成，下一步进入多平台字体与真实用户试点。
6. **本轮编辑门禁**：颜色/编码编辑、全局筛选器、十六进制颜色安全校验和版本对比已完成。

## 6. 本轮进展

- 已完成数据中心按所选记录、项目或类型构造 `DataScope`；
- 已完成一键进入 AI 助手并预填 `@数据可视化专家`；
- 已完成聊天区数据范围提示、清除操作和请求透传；
- 已增加端到端 UI 门禁 `smoke-data-visualization-handoff.mjs`。
- 已完成两个专家的 @Mention 候选、过滤和键盘选择；
- 已完成 8 个真实生成阶段的 IPC 事件和聊天进度展示；
- 已增加阶段顺序测试与 @Mention UI 门禁。
- 已完成组件数据口径抽屉、固定 1920×1080 PNG 导出和导出治理确认；新增 `smoke-dashboard-governance.ts` 覆盖范围、查询字段和敏感字段识别。
- 已完成本地草稿自动保存、恢复与清除，新增 `smoke-dashboard-drafts.ts` 覆盖草稿格式和异常降级。
- 已完成自然语言 Patch 闭环：活动大屏识别、受限操作解析、局部查询重算、校验失败回滚和聊天/工作台同步；新增 `smoke-dashboard-patch.ts`。
- 已完成画布拖拽/缩放和布局校验；已完成高级查询面板，支持维度、时间粒度、指标、筛选、排序和 limit 的安全编辑。
- 已补充拖拽组件间换位：拖拽组件中心进入目标组件时交换位置并高亮目标，尺寸不兼容或越界时保留原布局；`smoke-dashboard-editor.ts` 增加换位回归。
- 已完成字段画像持久化、默认语义角色、业务显示名/别名更新 IPC；已完成 QuerySpec 稳定哈希缓存和数据修订号失效；新增 `smoke-analytics-cache.ts` 覆盖命中、失效和语义持久化。
- 已完成 QueryMeasure 的同比、环比、占比和累计计算及查询面板控制；新增 `smoke-query-calculations.ts` 覆盖跨年、跨月、缺少对比周期和累计结果。
- 已完成 QueryMeasure 的 N 周期差值/百分比对比和受限自定义四则公式；新增 `smoke-query-formulas.ts` 覆盖多周期、公式引用校验和安全解析。
- 已完成颜色与图表编码编辑、双指标折线/柱状渲染；新增全局筛选栏和最多 8 个筛选器的结构化持久化；新增 `smoke-dashboard-editor.ts` 覆盖颜色白名单、编码引用、筛选器操作符和 QuerySpec 形状。
- 已完成大屏操作审计表、IPC 查询和工作台审计抽屉；覆盖保存、恢复、诊断、三种大屏导出和数据 JSONL 导出的成功/取消/失败结果；新增 `smoke-dashboard-audit.ts`。
- 已完成工作台单组件自动修复：根据字段画像确定性修复失效维度、数值聚合、指标标识、图表编码、散点图第二指标和最小布局；查询执行和整屏校验均成功后才提交，并记录 `repair-component` 成功/失败审计；新增 `smoke-dashboard-repair.ts`。
- 已完成可视化 Agent 工具调用明细审计：生成和修改运行会记录六类受控步骤的顺序、状态、尝试次数、耗时和数量型元数据，质量抽屉默认折叠展示；日志不保存 Prompt 正文、筛选值和查询结果行，数据库读写均做白名单、长度、数值和损坏 JSON 校验。
- 已完成 Spec 快照指纹策略：版本表继续保存可恢复的完整 DashboardSpec，保存/恢复/诊断/导出/修复等审计只记录稳定的属性排序 SHA-256；组件修复同时记录源 Spec 与结果 Spec 指纹，避免在审计表重复持久化完整数据。
- 已完成版本回滚体验补强：恢复操作增加覆盖提示、恢复中状态和重复点击保护；新增 `smoke-dashboard-versions.ts` 并注册为 `smoke:dashboard-versions`，覆盖版本递增、恢复新版本和版本差异。

## 7. 本阶段发布门禁产物

- `src/main/experts/dashboard-golden.ts`：研发质量、交付进度、项目健康度三套脱敏记录与人工 DashboardSpec。
- `scripts/smoke-dashboard-golden.ts`：逐组件执行真实 QuerySpec，校验结构、数据非空和主题覆盖。
- `scripts/smoke-dashboard-performance.ts`：生成 50,000 条内存脱敏记录，执行 20 次冷查询并计算 P95，同时验证缓存命中。
- `src/shared/dashboard-visual-regression.ts` 与 `scripts/smoke-dashboard-visual-matrix.ts`：覆盖 1366×768、1920×1080、4K 的布局边界、标题空间、碰撞和主题基线。
- `scripts/smoke-dashboard-electron-visual-matrix.ts` 与 `scripts/smoke-dashboard-electron-visual.ts`：固定 Windows Electron 真实截图门禁，3 套黄金大屏在 3 个视口共 9 项 PNG diff 均通过；基线位于 `tests/visual-baselines/dashboard`。
- 版本抽屉新增 Vn→Vm 对比，字段、组件和 QuerySpec 口径变化可直接查看。
- `scripts/smoke-dashboard-versions.ts` 与 `scripts/smoke-dashboard-version-ui.mjs`：覆盖版本递增、恢复新版本、差异查看以及恢复前确认浮层。

当前基准结果：50,000 条记录查询 P95 在本机约 270～500 ms（门禁 < 2 s）；三套黄金大屏在三种分辨率均通过布局门禁和固定 Windows Electron PNG diff。真实 Electron 多平台字体基线与用户试点仍属于下一阶段验收工作。

下一开发入口：试点与发布验收，包括真实用户反馈、多平台字体截图基线、公式跨粒度扩展和版本回滚体验；团队权限、审批和集中审计仍需产品确认，不在当前单机单用户阶段自动实现。
