# 后续开发约束

## 匹配明细面板

- 关键功能信息词面板必须沿用应用当前主题变量（`--surface-raised`、`--surface-soft`、`--stroke`、`--accent`、`--text-main`、`--text-muted`）。深色主题下不得再使用白色或浅蓝色的局部背景，标题、边框、标签和操作按钮需要与紫色主色保持一致。
- 新增或调整匹配明细面板的颜色时，优先修改主题变量或面板作用域样式，不要在组件内写与主题冲突的固定浅色值。

## 匹配明细名称详情弹窗

- 点击匹配明细列表中的数据名称查看详情时，应保持当前匹配抽屉的上下文，避免叠加多个强遮罩造成页面层级混乱；必要的嵌套详情浮层需要使用清晰的层级、边界和关闭入口。
- 详情弹窗应使用响应式宽度，并根据主应用可用高度限制内容区高度；超出内容必须在弹窗内部滚动，不得把主页面或匹配抽屉整体撑高。
- 弹窗标题需要区分“数据中心记录”与具体名称；名称过长时使用省略显示。元数据标签必须有足够宽度，禁止中文标签逐字换行。
- 数据中心描述中的 HTML 必须经过安全清洗后再渲染，禁止把 `<p>` 等原始标签直接作为文本展示；描述内容、空状态和图片引用需要有明确的可读样式。
- 弹窗内不得出现白色或浅色内容块；原始 JSON 等低频信息默认折叠，并使用深色主题背景和弹窗内部滚动。

## 列表与列宽

- 列表默认列宽应适中，避免为了展示少量文字让列表产生过长的横向滚动；长文本使用截断、换行或展开交互处理。
- 需要用户查看的列表列默认支持拖拽调整。拖拽手柄需要有明确的 hover/focus 状态，并同时支持键盘调整。
- 系统内所有可见列表和表格（包括数据中心、资产中心、知识库、项目管理、数据采集/推送、同步配置、请求日志、聊天数据等）都必须接入统一的列宽拖拽能力；复选框、展开按钮等系统生成的辅助列可以不提供拖拽手柄，但业务数据列不能遗漏。已有自定义表格也必须达到同等的拖拽、键盘、反馈和持久化标准。
- 用户调整后的列宽必须持久化缓存；读取缓存时要校验数值并限制在合理的最小/最大范围内，缓存损坏或不可用时回退到安全默认值。
- 列宽缓存键需要带版本号；修改列定义或默认范围导致旧缓存不兼容时，应升级版本号而不是直接复用旧数据。

## 表格尺寸与滚动

- 所有表格默认保持适中尺寸，避免无边界地撑大页面；优先通过合理的行内边距、文本截断/换行和分页控制信息密度。
- 所有表格必须根据主应用可用高度设置响应式最大高度，不能只依赖内容自然撑开；高度规则应随窗口高度变化，并在小窗口下保留可用的最小高度。
- 超出最大高度的内容应在表格内部（`.ant-table-body`）出现纵向滚动条，尽可能避免右侧主页面出现纵向滚动条；新增表格需要配置 `scroll.y`，不能只配置横向 `scroll.x`。
- 页面级布局需要为表格预留稳定空间；表格所在卡片、Tabs 内容区和抽屉/弹窗内部需要设置 `min-height: 0`，避免子表格把父级撑出主应用高度。
- 抽屉、弹窗和详情页可根据自身可用空间使用更紧凑的最大高度，但仍必须基于主应用高度响应式计算，不得写死一个只适配单一分辨率的高度。

## 状态条与数量展示

- 列表内使用进度条、状态条或占比条时，不要在条旁重复显示同一状态文字；默认只展示状态条，完整文字通过 Tooltip 提供，并保留 `aria-label` 等可访问语义。
- 数量与单位（例如“2 条”）必须保持在同一行，禁止因网格列数或样式设置导致数字和单位上下换行。

## Tab 标签间距

- 所有 Tab 标签内部需要保留足够的上边距，避免文字、图标与 Tab 底部激活线或边框发生视觉重叠；新增或覆盖 Tab 样式时不得移除该安全间距。

## 提交前检查

- 在深色主题下检查面板背景、边框、文字、标签、输入框和按钮的对比度。
- 验证列宽拖拽、键盘调整、刷新/重新打开后的宽度恢复，以及窄窗口下的横向滚动行为。
- 逐一检查系统内所有列表的业务列都出现可操作的调整手柄，调整后刷新或重新打开仍能恢复宽度，并确认固定列、操作列和选择/展开辅助列没有遮挡或交互冲突。
- 验证不同窗口高度下表格最大高度、表格内部纵向滚动和主内容区滚动条行为。
- 验证状态条旁没有重复文字，以及数量和单位在窄窗口下仍保持同一行。
- 验证从匹配明细名称打开的详情弹窗没有多层遮罩混乱、原始 HTML 标签不外露、标签不逐字换行、长内容只在弹窗内部滚动且主题颜色一致。
- 运行 TypeScript 检查和与项目管理相关的 smoke test；不要因局部 UI 调整破坏现有项目管理流程。

## 统一设计规范（VISSLM 工作台 v1）

后续所有功能开发、新功能页面开发和 UI 调整，必须先读取本文件，并沿用以下设计约束。设计目标是让 VISSLM Agent 保持统一的深色数据工作台体验：内容优先、紫色作为唯一主操作色、信息密度可控、页面在全屏和窄窗口下都保持稳定。

### 1. 设计令牌与颜色

- 页面和组件必须优先使用现有主题变量：`--surface-base`、`--surface-raised`、`--surface-soft`、`--surface-elevated`、`--surface-overlay`、`--stroke`、`--stroke-strong`、`--text-main`、`--text-muted`、`--accent`、`--accent-soft`、`--state-success`、`--state-info`、`--state-warning`、`--state-error`。
- 禁止在应用工作台、卡片、表格、表单、抽屉和弹窗中新增白色、浅灰或浅蓝色的局部背景；需要区分层级时使用主题变量或透明白色叠加，不要写新的孤立色板。
- 紫色 `--accent` 只用于主要操作、选中态、焦点环、链接和关键数据强调；成功、信息、警告、错误分别使用对应 `--state-*` 变量，不能用颜色代替文字状态。
- 所有交互控件必须在 hover、focus-visible、active、disabled、loading 状态下有可识别的视觉变化；focus-visible 使用 `--focus-ring` 或等效的紫色 2px 外轮廓。

### 2. 间距、尺寸与页面骨架

- 统一采用 4px 基础网格，允许的间距阶梯为：4、8、12、16、20、24、32、40px。新页面不得随意出现 6、10、14、18、22 等非必要间距。
- 应用窗口标题栏高度为 40px，左侧导航宽度为 224px；侧栏只保留一个主品牌标识，窗口标题栏只显示低强调度的应用名，禁止在同一角落重复堆叠图标、标题、副标题和发光装饰。
- 主内容区使用统一左右 gutter（`--page-gutter`，随窗口宽度响应式变化），内容容器最大宽度为 `--page-max-width`（1600px）；页面标题、操作工具栏、Tabs、卡片和表格必须落在同一条内容轨道上。
- 页面标题区使用“标题 + 一行说明”的层级，标题区与正文间距为 20px；正文区域使用 `.page-stack`，模块间距为 20px。顶部操作行使用 `.page-toolbar`，最小高度 36px，按钮间距 8px。
- 页面、卡片、Tabs 内容区、抽屉和弹窗内的表格父级必须设置 `min-height: 0`；表格内容必须配置响应式 `scroll.y`，纵向滚动在 `.ant-table-body` 内完成。
- 页面不应靠自然内容无限撑高；内容较长时优先使用表格内部滚动、分页、折叠或弹窗内部滚动，避免主页面出现无边界的纵向撑开。

### 3. 基础组件规范

- 卡片：默认圆角 12px、边框使用 `--stroke`、背景使用 `--surface-raised`，默认内边距 18px；小卡片内边距 14px。卡片标题高度约 52px，标题与操作区应在同一基线。
- 按钮：默认高度 36px、圆角 8px、字重 600、水平内边距 13px。主按钮使用紫色渐变和轻阴影；默认按钮使用 `--surface-soft`；文本/链接按钮不显示厚重背景；危险按钮使用 `--state-error` 的透明底和边框。
- 按钮状态：hover 只改变边框、背景和文字，不得造成布局跳动；active 可向下 1px；disabled 必须降低对比度并禁止 hover 反馈；loading 保留原按钮宽度，避免文字跳动；icon-only 按钮必须有 `aria-label`。
- 表单：统一使用 `layout="vertical"`。标签字号 12px、字重 600、标签到控件间距 6px；表单项默认下间距 18px；说明/错误文字字号 11px、行高 1.5。相关字段使用两列或三列网格，但在 1000px 以下自动降为两列，680px 以下降为单列。
- 输入控件：默认高度 36px、圆角 8px；placeholder 使用 `--text-muted` 的低对比度；hover 提升边框对比度；focus-visible 使用紫色焦点环；错误和警告状态分别使用 `--state-error` / `--state-warning` 边框。
- 搜索框：`Input.Search` 默认高度固定为 36px，常规筛选区宽度默认不超过 360px；搜索框内部输入控件不得再次继承外层最小高度。搜索框与相邻下拉框必须共用同一 36px 基线。
- 筛选下拉框：筛选区必须显式声明实际 `width`（例如 120、140、160、220px），禁止只写 `minWidth` 后依赖全局样式；只有表单字段内的下拉框允许默认满宽。
- Tabs：标签内部必须保留足够的上、下内边距，默认至少 `10px 12px 12px`；激活线与文字不得重叠；标签之间使用 4px 以上间距；页面级 Tabs 的内容区与标签导航之间统一保留 16px 顶部间距；当前 Ant Design 版本内容容器使用 `.ant-tabs-body-holder`，不得只覆盖旧的 `.ant-tabs-content-holder`；Tabs 内容区必须允许子表格收缩。
- 标签和状态：标签只用于短状态、类型和分类，不在进度条旁重复显示同一状态文字；数量与单位必须通过 `white-space: nowrap` 保持同一行。
- 状态胶囊：全局状态胶囊统一高度 24px、圆角 999px、水平内边距 8px、字号 11px；自定义状态胶囊必须复用同一组尺寸变量，不得通过局部 padding 造成高低不一。
- 执行进度：项目详情的执行中进度使用环形进度条展示百分比；阶段说明和当前执行文本放在环形进度旁，不能再使用横向进度条与重复百分比文本。

### 4. 列表、表格与可调整列

- 表格默认使用 12px 表头、10px 行内垂直间距和适中的列宽；长文本默认截断并通过 Tooltip、展开或详情入口查看全文。
- 需要用户查看的列必须支持拖拽调整；拖拽手柄宽度至少 10px，hover/focus-visible 清晰可见，并通过左右方向键以 12px 步长调整。
- 列宽缓存键必须包含版本号（例如 `:v1`）；读取缓存时必须校验数字并 clamp 到最小/最大宽度，缓存损坏或不可用时回退到默认值。修改列定义或范围时升级版本号。
- 表格需要同时考虑横向和纵向滚动：横向只为确实需要的列保留，纵向必须设置 `scroll.y`，并根据 `100vh` 计算最大高度。表格所在卡片、Tabs、抽屉和弹窗必须设置 `min-height: 0`。
- 进度条只表达视觉比例，完整状态文字放到 Tooltip 和 `aria-label`；不要在进度条右侧重复写相同百分比或状态词。

### 5. 抽屉、弹窗与详情内容

- 详情浮层必须保持触发它的页面上下文；匹配抽屉内打开数据中心记录时，详情弹窗使用 `mask={false}` 或等效的弱遮罩，明确层级、关闭入口和标题，不得叠加多个强遮罩。
- 抽屉/弹窗宽度必须响应式：桌面使用内容需要的最大宽度，窄窗口使用 `min(宽度, calc(100vw - 32px))`；内容区最大高度基于 `100vh` 计算，超出内容只能在浮层内部滚动。
- 标题必须区分业务域和具体名称，例如“数据中心记录 · 具体名称”“知识库文档 · 文件名”；名称过长省略显示，元数据标签必须禁止逐字换行。
- 描述中的 HTML 必须先安全清洗再渲染；禁止直接展示 `<p>` 等原始标签、危险属性或外部图片。没有描述、图片缺失和图片列表为空时必须有明确空状态。
- 原始 JSON、调试请求等低频信息默认折叠，使用深色代码背景和内部滚动；详情浮层内不得出现白色或浅色内容块。

### 6. 页面类型模板

- 数据概览：顶部为 4 个同高指标卡，下方为单一“数据构成”分析卡；指标卡使用统一图标槽位和数字层级，不新增大面积装饰。
- 数据列表/资产中心/知识库：标题说明 → 操作工具栏 → 筛选区 → 表格。筛选控件同一行时使用 8/12px 间距，窄窗口自动换行；详情在抽屉内展示。
- 表单/配置页：标题说明 → Tabs → 卡片内分组表单 → 操作区。主提交按钮固定在表单末尾，测试/辅助动作放在其后；表单不应跨越整屏宽度，建议最大 820px。
- AI 助手：历史会话与当前会话保持两栏结构；消息区和输入区都要有明确边界，输入区固定在会话卡底部，发送按钮和快捷键说明保持同一行。
- 数据采集/推送：标题说明 → 顶部动作 → 配置 Tabs → 配置卡 → 数据选择/请求日志表格。高级参数默认折叠，调试 JSON 默认折叠。
- 项目管理：列表页和项目详情页保持同一内容轨道；匹配抽屉使用主题变量、内部表格滚动和可调整列；匹配名称详情继续保持上下文，不新增独立白底详情页。

### 7. 响应式与无障碍验收

- 在全屏、1200px、1000px、760px 和 680px 附近检查内容轨道、工具栏换行、表单列数、横向滚动和弹窗宽度；不得出现内容贴边、遮挡、页面被子表格撑高或操作按钮被裁切。
- 所有可点击元素必须有键盘焦点态；拖拽列宽必须支持键盘；图标按钮、状态条、图片和关闭入口必须有可访问名称或 `aria-label`。
- 深色主题验收顺序：背景层级 → 边框 → 正文/次要文字 → 输入控件 → 主要/危险按钮 → hover/focus/disabled；对比度不足时优先调整主题变量，不在单个组件内写临时浅色值。

### 8. 每次提交前必须检查

- 先确认本文件已被读取，再开始新增页面或组件。
- 运行 TypeScript 检查和与项目管理相关的 smoke test；UI 调整至少检查数据概览、资产中心、AI 助手、数据采集、项目管理、系统配置，以及匹配抽屉和详情浮层。
- 验证全屏下所有页面使用同一左右边距和内容最大宽度；验证品牌元素没有在窗口标题栏和侧栏重复堆叠。
- 验证表格内部纵向滚动、列宽拖拽/键盘调整/刷新恢复、窄窗口横向滚动、Tab 安全上边距、状态条无重复文字、数量和单位不换行。
- 验证详情弹窗不会产生多层遮罩混乱、HTML 标签不外露、标签不逐字换行、长内容只在弹窗内部滚动且所有颜色符合深色主题。
