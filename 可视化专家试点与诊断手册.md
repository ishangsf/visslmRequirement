# 数据可视化专家试点与诊断手册

## 1. 试点目标

阶段 5 使用 5 至 10 名真实用户验证以下指标：

- 用户能否在不需要研发介入的情况下生成、修改、保存和导出大屏；
- 首次生成是否在 90 秒内完成，二次修改是否在 30 秒内完成；
- 生成的大屏是否能说明数据范围、指标口径和异常原因；
- 用户是否能通过质量诊断自行定位空结果、慢查询和字段问题。

## 2. 推荐试点角色

- 2 名项目经理：验证项目进度、风险和发布质量场景；
- 2 名测试负责人：验证缺陷、测试执行和质量趋势场景；
- 1 至 2 名数据管理员：验证字段口径、敏感数据和导出范围；
- 1 至 2 名研发负责人：验证性能、稳定性和故障恢复。

每名用户至少完成一次“生成、编辑、保存、恢复旧版本、导出 PDF”的完整流程。

## 3. 试点前检查

1. 数据采集已完成，目标项目至少包含一个时间字段和一个低基数字段。
2. Ollama 模型连接正常，推荐关闭思考模式。
3. 在数据中心确认项目、类型和记录数量符合预期。
4. 使用固定回归集执行离线门禁：

   ```powershell
   npx tsx .\scripts\smoke-visualization-regression.ts
   ```

5. 执行核心、版本和质量诊断测试：

   ```powershell
   npx tsx .\scripts\smoke-visualization-core.ts
   npx tsx .\scripts\smoke-dashboard-versions.ts
   npx tsx .\scripts\smoke-dashboard-quality.ts
   ```

6. 在 Electron 调试预览已使用 `9223` 端口启动时，执行阶段 5 桌面 UI 回归：

   ```powershell
   $env:VISSLM_CDP_PORT='9223'
   node .\scripts\smoke-stage5-ui.mjs
   ```

   回归会在 1280×800 下检查质量抽屉、查询数据列表和记录详情是否完整显示，并输出截图路径。

## 4. 质量诊断口径

工作台顶部“质量”按钮会重新执行每个组件的受控 `QuerySpec`，并输出：

- 结构错误：DashboardSpec、布局、字段或编码不合法；
- 空结果：筛选后没有可展示数据；
- 截断：结果达到组件查询上限；
- 慢查询：单组件执行时间超过 2 秒；
- 敏感字段：字段名疑似包含 Token、手机、邮箱、身份证等信息；
- 图表风险：环图分类过多、标题过长、重复查询或业务上下文缺失。

质量分从 100 分开始，错误扣 20 分、警告扣 6 分、建议扣 1 分。该分数用于试点比较，不替代业务人员对指标正确性的确认。

## 5. 常见故障

### 当前数据范围没有可用字段

先完成数据采集，或在数据中心调整项目、类型和记录筛选范围。

### DashboardSpec 校验失败

查看质量诊断中的组件编号和错误信息。常见原因是字段不存在、数值聚合引用文本字段、组件重叠或图表编码没有引用指标 ID。

### 查询结果为空

检查 DataScope 和组件筛选条件。空结果不是零值，不应直接解释为业务指标为零。

### PDF 内容不完整

先进入预览确认所有组件正常渲染，再导出 PDF。长标题和过多分类应先根据质量建议调整。

### 生成耗时过长

在质量抽屉查看“最近生成运行”。若多次超过 90 秒，减少数据范围、关闭思考模式，并确认本地模型没有被其他任务占用。

## 6. 反馈记录

每次试点记录用户角色、业务目标、数据范围、生成耗时、重试次数、质量分、修改次数、版本恢复和导出结果。

不得在反馈中粘贴 API Token、完整敏感记录或未脱敏的导出文件。
