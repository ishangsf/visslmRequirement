# VISSLM 后续开发约束

本文件是项目约定的设计规范入口。后续所有功能开发、新页面开发和 UI 调整，必须先读取本文件；完整的仓库级约束与本规范正文同步维护在同目录的 `agents.md`。

## 统一设计规范（VISSLM 工作台 v1）

### 设计令牌

- 优先使用现有主题变量：`--surface-base`、`--surface-raised`、`--surface-soft`、`--surface-elevated`、`--surface-overlay`、`--stroke`、`--stroke-strong`、`--text-main`、`--text-muted`、`--accent`、`--accent-soft`、`--state-success`、`--state-info`、`--state-warning`、`--state-error`、`--focus-ring`。
- 工作台、卡片、表格、表单、抽屉和弹窗内禁止新增白色、浅灰或浅蓝局部背景；禁止用组件内固定色值破坏深色主题。
- 紫色只用于主操作、选中态、焦点环、链接和关键强调；成功、信息、警告、错误使用对应状态变量。

### 页面骨架与间距

- 采用 4px 基础网格，只使用 4、8、12、16、20、24、32、40px 间距。
- 标题栏固定 40px，侧栏固定 224px；主品牌标识只保留在侧栏，标题栏只显示低强调度应用名，不重复堆叠品牌图标。
- 主内容区统一使用 `--page-gutter` 和 `--page-max-width: 1600px`；标题、说明、操作行、Tabs、卡片和表格使用同一内容轨道。
- 页面标题采用“标题 + 一行说明”，标题区与正文间距 20px；`.page-stack` 模块间距 20px；`.page-toolbar` 最小高度 36px，按钮间距 8px。
- 页面、卡片、Tabs、抽屉和弹窗内的表格父级必须 `min-height: 0`；表格必须配置响应式 `scroll.y`，纵向滚动只在 `.ant-table-body` 内完成。

### 组件、表单和控件

- 卡片：圆角 12px、主题边框、主题背景、默认内边距 18px；小卡片 14px；标题区约 52px。
- 按钮：高度 36px、圆角 8px、字重 600、水平内边距约 13px。主按钮使用紫色主色；默认按钮使用 `--surface-soft`；危险按钮使用错误色透明底和边框。
- 按钮必须覆盖 hover、focus-visible、active、disabled、loading 状态；hover 不得引起布局跳动；active 可下移 1px；图标按钮必须有 `aria-label`。
- 表单统一 `layout="vertical"`；标签 12px/600，标签与控件间距 6px，表单项间距 18px；说明和错误文字 11px/1.5。
- 输入框、选择器、日期控件默认高度 36px、圆角 8px；hover 提升边框对比度；focus 使用紫色焦点环；错误和警告使用对应状态色。
- 搜索框默认高度固定为 36px，常规筛选区宽度默认不超过 360px；搜索框内部输入控件不得再次继承外层最小高度，必须与相邻下拉框共用同一基线。
- 筛选下拉框必须显式声明实际 `width`，禁止只写 `minWidth` 后被全局规则拉满；只有表单字段内的下拉框允许默认满宽。
- Tabs 标签必须保留至少 `10px 12px 12px` 内边距，激活线不得与文字重叠；页面级 Tabs 内容区与标签导航之间统一保留 16px 顶部间距；当前 Ant Design 版本使用 `.ant-tabs-body-holder` 作为内容容器，不得只覆盖旧的 `.ant-tabs-content-holder`；Tabs 内容区允许子表格收缩。
- 状态标签只表达短状态、类型和分类；进度条旁不得重复同一状态文字；数量和单位必须 `white-space: nowrap` 保持同一行。
- 状态胶囊统一高度 24px、圆角 999px、水平内边距 8px、字号 11px；自定义状态胶囊必须复用统一尺寸变量。
- 项目详情执行中状态使用环形进度条展示百分比，阶段说明和当前执行文本放在环形进度旁，禁止再用横向进度条重复展示百分比。

### 列表、表格与浮层

- 长文本默认截断并通过 Tooltip、展开或详情入口查看；列宽需支持拖拽和键盘调整，并使用带版本号的缓存保存，读取时校验并限制最小/最大值。
- 表格需要同时考虑横向和纵向滚动；最大高度基于 `100vh` 响应式计算，避免主页面被内容无限撑高。
- 详情抽屉/弹窗必须保持触发页面上下文；嵌套详情使用弱遮罩或 `mask={false}`，响应式宽度和基于 `100vh` 的内部滚动。
- 标题要区分业务域与具体名称，长名称省略；元数据标签禁止中文逐字换行；HTML 描述必须安全清洗后渲染；原始 JSON 默认折叠并使用深色内部滚动区域。

### 页面模板与验收

- 数据概览：统一同高指标卡 + 单一数据构成卡；列表/资产中心：标题说明 → 操作行 → 筛选 → 表格；表单页：标题说明 → Tabs → 分组表单 → 操作区。
- AI 助手保持历史会话与当前会话两栏；数据采集/推送保持标题说明 → 动作 → 配置 Tabs → 配置卡 → 日志表格；项目管理与匹配抽屉沿用主题变量和内容轨道。
- 每次改动前后必须检查全屏、1200px、1000px、760px 和 680px 附近的边距、换行、滚动、按钮可见性、焦点态、深色对比度，以及品牌元素是否重复堆叠。
- 提交前运行 TypeScript 检查和项目管理 smoke test；UI 调整至少回归数据概览、资产中心、AI 助手、数据采集、项目管理、系统配置以及详情浮层。
