# VISSLM Agent v1.5.1

本版本基于当前工作区发布，包含资产删除修复及项目管理、AI 助手和可视化大屏的累计更新。

- 修复资产中心选中删除、全部删除时，需求匹配候选引用导致的 FOREIGN KEY constraint failed。对应引用和资产在同一事务中删除，失败时回滚；保留项目需求、匹配运行及其他资产的候选结果。
- 更新项目需求追踪、项目导出与匹配界面。
- 更新可视化大屏场景向导、组件配置和领域数据适配。
- 更新 AI 助手区域需求查询能力。

Windows x64 安装包包含本地向量模型、重排模型、OCR 和 VC++ 运行库。

验证：TypeScript 检查、删除专项回归、项目管理及项目导出 smoke、更新器 smoke、匹配界面静态检查、大屏组件配置/场景就绪/场景生成测试、区域需求回归、完整构建、安装包启动和模型文件哈希校验通过。

验收状态：ACCEPTED_WITH_RISKS。项目管理实时界面 smoke 因未连接调试端口未执行，静态检查通过。安装包未配置商业代码签名，Windows 可能提示未知发布者。
